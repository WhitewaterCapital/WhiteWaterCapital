import { ModuleShell } from "@/components/ModuleShell";

// INTRA / EXITUS — entry and exit. Empty shell; build the levels model.
export default function IntraExitusPage() {
  return (
    <ModuleShell
      name="Intra / Exitus"
      latin="enter · exit"
      title="Where to get in, where to get out."
      intro="Entry zone, stop, targets, sizing, and time-stops for a trade."
    />
  );
}
